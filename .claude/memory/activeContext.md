# Active context · HapieCoin

**Current phase**: Phase 4 "Accounts, billing, admin, public site" is COMPLETE and merged (PRs #14–#20, 08–09 Sep 2026; ADR-030..037): plans + entitlements, Razorpay checkout + coupons + invoices, referrals + commissions, admin users / banners / promotional emails, assistant + product tour, public payoff preview, public-site audit. All 268 Phase 4 traceability rows carry build evidence. Phase 5 (analytics, alerts, journal, share pages, header market stats, shortcuts) is next; its Market Analytics block (~250 rows) needs a data-source decision from the user before it can start. Still pending from the user: IP cleanup PR (purge `mockup-clone/` from history), optional `RAZORPAY_WEBHOOK_SECRET` + public webhook URL (GAPS #49).

**Last updated**: 2026-09-09 (PR #26 merged; adjustment workbench APPROVED (H2 + six extras, ADR-044) and in build: A1 + A2 MERGED (PR #27 workbench → adjust-api, PR #28 adjust-api → main a8179c3); A3 live confirm + extras BUILT on `feat/adjust-live` (awaiting gate + Playwright + /commit); then Analyse tabs, then 5.5 terminal)

## Recent
- 09 Sep (late): PR A3 built on `feat/adjust-live`: limit entries end to end (schema `OrderType`, `AdjustBody.orderType`, `StrategyOrder.orderType`; venues `PlaceOrderInput.orderType/limitPrice` real + fake; executor `placeEntries(..., orderType)`; `strategy_orders.order_type` migration 0009_cold_leech; mock `placeLive` limit rests → pending); web `AdjustConfirmDialog` rewrite (band per leg, order type seg, `HoldButton` 1.2 s, results view with fill states + Done, notional-over-balance rail), `PlansBar` + `useDraftFigures`, `lib/adjust/fixes.ts` + `QuickFixes` (ranked), scenario slider, `riskAlerts` store stub; tests in trading-live / workbench / model / store / api live / venues; e2e HC-TR-152 live hold test + visual analyse-adjust-live; spec rows 149/151/152 evidence, 153/154 working; ADR-044 as-built A3.
- 09 Sep (night): PR A2 built on `feat/adjust-workbench` (from `feat/adjust-api`): pricing `analyze({ valuationMs })` + `curveExtremes` (pricing 88 tests, 100 % lines); web `lib/adjust/model.ts` + `useAdjustWorkbench.ts`, store `adjust` slice (openAdjust / closeAdjust / updateAdjust, not persisted), `useStrategyAnalysis` adjusting mode (`adjusting`, `openLegs`, `before`, `markOf`, `quoteVersion`, `valuationMs`), `ChainPickerBody` (extracted from the dialog; keyboard opt-in; held pills), `components/adjust/{AdjustWorkbench,PositionTicket,AdjustConfirmDialog}.tsx`, `BeforeAfterStrip`, payoff ghost curve, Details history + `AdjustedBadge` + `CardFigures`, cards' Adjust button + key A, Workspace left-pane swap, `ConfirmAdjustmentDialog.tsx` removed. Tests: model.test.ts, workbench.test.tsx, trading / trading-live rewritten (web 407 tests); e2e block HC-TR-148..152 + visuals analyse-workbench / analyse-adjust-confirm; spec rows HC-TR-148..154 via 21-trading.json + buildspec; ADR-044 as-built A2 note. Code review fixes: workbench reads open legs from its own hook and the draft is discarded when the pane stops following the strategy (store), live Place disabled until the venue check passes, engine lower tail = exact limit at price 0, "today" value-at chip, 502 → refresh + reset draft, empty lots field ignored, Enter/Esc on the box only, card figures price once per mount. Gate 34/34, Playwright 86/86, web 407 tests.
- 09 Sep (late night, later): PR A1 built on `feat/adjust-api`: schema `AdjustBody` / `AdjustChange` / `StrategyAdjustment` + `Strategy.adjustments` + `LivePreviewBody.adds/changes`; table `strategy_adjustments` (migration 0008_fair_zombie); route `POST /v1/strategies/{id}/adjust` (paper in one pass, live exits-then-entries with band, idempotency by batch id, reason as history, cap after closes, all live checks before the first order, per-fill booking, partial batch recorded as `Stopped:`, unique (strategy, batch) key written first, `StrategyOrder.batchId`); `/live/preview` overrides via `adjustPreview` (`preview()` takes `exits`); `PlanLeg` type in live-exec; mock API parity (`/strategies/:id/adjust`, preview overrides, `adjustments: []`); tests: schema +1, api strategies +3, live +3 (api 234, schema 187, web 394, all at floors). ADR-044 as-built note; design doc §3/§4 updated. Dev DB needs `pnpm --filter @hapiecoin/api db:migrate` (main.ts migrates on start).
- 09 Sep (late night): Adjustment workbench designed and approved. Two clickable mockups (v1 A/B, v2 H1/H2) reviewed with the frontend-design skill, a Plan agent and a UX-critic agent; the user chose H2 · Workbench with the six extras (plans compare, scenario date, fix ranking, keyboard, alert stub, mobile) and lots editing in both places (lots-after stepper on open legs; ADDS / TRIMS / CLOSES / FLIPS effects). Saved: docs/design/adjustment-workbench.md, docs/design/mockups/adjustment-workbench-v{1,2}.html, ADR-044. Build plan: A1 API (`/adjust` batch, preview overrides, band, idempotency, reason) → A2 workbench + paper confirm → A3 live confirm + extras.
- 09 Sep (night, `feat/analytics-whales`, uncommitted): Phase 5 item 5 PR 5.4b (ADR-043, docs/design/analytics.md §5.4b, GAPS #60): schema `whales` dataset (+ whaleIndex), ingest Hyperliquid adapter (leaderboard scan, clearinghouseState, mark prices; POST support in JsonClient), Bybit orderbook, `whales.ts` tracker (poll diffs → alerts, walls with first-seen, hourly activity via appendSeries), job every 60 s, config HYPERLIQUID_* / WHALE_* / WALL_USD; web `/analytics/whales` (tiles + gauge, alerts feed, index chart, positions and large-order tables, reserves coming soon); SOON map now empty. Verified: schema 186 tests, ingest 59 (100/99.83/100/100), web 394 (93.7/87.3/91.1/95.2), Playwright 85/85 (+1 capture ×2 themes), gate 34/34, fact-checker 10/10; live: 54 of 200 scanned wallets hold positions, top ETH short $241M, 73 Bybit walls. Merged as PR #26.
- 09 Sep (evening, `feat/analytics-options-sentiment`, uncommitted): Phase 5 item 5 PR 5.4a (ADR-042, docs/design/analytics.md §5.4a, GAPS #59): schema datasets `options` / `cycle` / `rsi` / `premium` + maths (maxPain, rsi, sma, logLinearFit, rainbowBand); ingest adapters Deribit + Delta (thin), Bybit klines, CoinGecko exchange tickers, store `appendSeries`, jobs buildOptions / buildCycle / buildRsi / buildPremium; web `/analytics/options` (exchange chips, expiry chart with amber max pain, donut, table), `/analytics/sentiment` (F&G bands, checklist with n/a rows, Pi Cycle / rainbow / 2Y MA on log axes, premium bars, RSI screener), `/analytics/etf` coming-soon layout; Chart gains logY / bands / regions / labels. Live: Deribit $34.8B OI, Delta $672M, cycle 1000 pts, rsi 10 rows, premium first point. Verified: schema 185 tests (100/98.99/100/100), ingest 53 (100/99.79/100/100), web 392 (93.6/87.2/91.0/95.2), Playwright 84/84 (+3 captures ×2 themes), gate 34/34, fact-checker 12/12. Merged as PR #25.
- 09 Sep (evening, `feat/ingest-fallbacks`, uncommitted): ADR-041 venue fallbacks in the ingest: `buildLongShort` Binance → Bybit account ratio; generic `LiquidationStream` with Binance and Bybit specs (`allLiquidation.{SYMBOL}USDT`, subscribe + 20 s ping), `BYBIT_WS_URL`, health `stream: { binance, bybit }`; web empty-state copy "Binance or Bybit". Taker volume (OKX units unverified) and basis remain in GAPS #57. Verified: ingest 49 tests 100/99.75/100/100, web analytics unit 16, Playwright analytics.spec 6/6, gate 34/34, fact-checker 9/9; live on the user's machine long-short:BTC via Bybit (168 pts) and 43 Bybit liquidation fills within 20 min. Merged as PR #24.
- 09 Sep (afternoon, `feat/analytics-pages`, merged as PR #23): Phase 5 item 5 PR 5.3 (ADR-040, docs/design/analytics.md §5.3, GAPS #57/#58): `/analytics/markets` screener (category chips via ?category=, ★ Watchlist chip, hidden 7d %/Funding/Liq 24h, Cmp checkbox column in DataTable → ComparePanel, ?compare=), `/analytics/derivatives` (CoinSelect, four tf charts incl. close price from the CoinGecko sparkline, Basis coming soon, funding-arbitrage table via useAnalyticsMany), `/analytics/liquidations` (tiles, 1h/4h/12h/24h windows, HBars by exchange, top-coins table, feed with Min USD), coin page charts + LiqHeatmap + per-exchange tables; pure helpers in `lib/analytics/derive.ts`; mock snapshots for all per-symbol datasets + liquidations (NOW = current minute). Network finding: on this machine fapi.binance.com and www.okx.com reset TLS (only Bybit reachable) → long-short / taker-volume jobs fail, liquidation feed empty; Bybit fallbacks proposed in GAPS #57. PR 5.2 merged as PR #22 (main 00f9b1e); COINGECKO_API_KEY set by the user, markets dataset live locally.
- 09 Sep (day, `feat/analytics-web`, merged as PR #22): Phase 5 item 5 PR 5.2 (ADR-039, docs/design/analytics.md §5.2): `overview` dataset folded by the ingest (`apps/ingest/src/overview.ts`), web `lib/api/analytics.ts` hooks, `lib/analytics/{chart,format}.ts`, components/analytics (AnalyticsShell with sections + coin search + Terminal ▾, Chart SVG, DataTable, Tile/Panel/SourceLine/Gauge/Treemap/ComingSoon), routes /analytics → /analytics/hub, /analytics/overview, /analytics/coin/:symbol (tiles only), section and /terminal placeholders inside the shell, palette nav:analytics-hub/overview, store.watchlist. Mock API serves deterministic snapshots (test/mock-analytics.ts). Next: PR 5.3 liquidations / derivatives / screener / coin charts.
- 09 Sep (day, `feat/analytics-ingest`, uncommitted): Phase 5 item 5 PR 5.1 (ADR-038, docs/design/analytics.md, GAPS #55/#56): `packages/schema/src/analytics.ts` (7 dataset envelopes), new `apps/ingest` (Binance / Bybit / OKX / CoinGecko / alternative.me adapters, JsonClient with retry, LiquidationBuffer + Binance forceOrder stream, Scheduler with stale marking, Redis/Memory store, /healthz), API `GET /v1/analytics/{dataset}` (public, cache + X-As-Of, 503 until ingested) with `analytics` on AppDeps. Next PRs: 5.2 analytics shell + hub + overview pages, 5.3 liquidations / derivatives / coin, 5.4 options / sentiment / whales + coming-soon panels, 5.5 terminal.
- 09 Sep (early morning, `feat/public-site`, uncommitted): Phase 4 item 5c evidence audit: 34 public rows + plan-banner rows tagged with their tests, new `landing-sections.test.tsx`, GAPS #54 for the v2-brief landing extras not built. Item 5 complete on the worktree; needs rebase onto main after PR #19, then /commit → PR #20.
- 09 Sep (night, `feat/public-site`, uncommitted): Phase 4 item 5b (ADR-037, GAPS #53, docs/design/public-site.md): public `/payoff-preview` route reusing PayoffChart on four fixed presets priced inline by `@hapiecoin/pricing` (`lib/payoff-preview.ts`), legend/layers/zoom/SD header/target + day sliders/summary, palette nav:payoff-preview (signed out too), footer Quick Link. Synthetic OI shape, fixed spot; live OI needs a public chain endpoint.
- 09 Sep (night, `feat/public-site` in worktree ../HapieCoin-item5, uncommitted): Phase 4 item 5a (ADR-036, docs/design/assistant-tour.md): HapieCoin Assistant (draggable launcher on every route, keyword answer bank `lib/assistant.ts`, strategy explainer from `useStrategyAnalysis`, visitor mode) and the 16-step product tour (`lib/tour.ts` data + `shell/Tour.tsx` overlay; `hc-tour:*` events from addLeg / TradeModeDialog / SaveDraftDialog / TradePreviewDialog / TradeFlow; auto-start once via `hapiecoin.tour`, replay from Settings → Take a tour and palette act:tour / act:assistant). Playwright fixture `tour: false` by default. Next in the same branch: 5b landing payoff preview, 5c public/legal evidence audit.
- 08 Sep (late night, `feat/admin-emails`, merged 09 Sep as PR #19): Phase 4 item 4c (ADR-035, docs/design/admin.md §4c): `campaigns` + `campaign_recipients` (migration 0007), `Mailer.sendPromo` (capture mailer records promos and can bounce), `emails.ts` (recipients by segment, per-recipient rendering, sequential delivery, history), routes `emails.ts`, web EmailsAdmin (Compose with templates / placeholders / preview / test send / confirm, History with sortable grid, Columns, CSV, detail), mock routes + `/__test/seed { campaigns: n }`. Fixed on the way: the admin row now falls back to the newest expired subscription so the Expired status is reachable (it never was). GAPS #50 (queue for large sends). Verified 09 Sep: gate 30/30 (--concurrency=3), api 224 tests (branches 90.24 %), web 326 unit (functions 90.19 %), schema 178, fact-checker clean; Playwright 61/62 on the clean re-run; the one failure (analyse legs survive a reload) passed on an immediate re-run of that spec and is recorded as GAPS #51 (flaky). Lesson: never let another test run start while Playwright is up. Next: Playwright, /commit → PR #19, then item 5.
- 08 Sep (late night, `feat/billing-checkout`, uncommitted): Phase 4 item 2 (ADR-034, docs/design/billing.md 'Checkout'): `coupons` + `payments` tables (migration 0006), `razorpay.ts` (fetch client + FakeRazorpay, HMAC checks), `coupons.ts` (one evaluation function), `checkout.ts` (order → pending row → settlePaid on verified confirm or webhook, cancel, invoices, ₹0 activation rows), routes `checkout.ts` + `coupons.ts`, `razorpay` on AppDeps, config RAZORPAY_* + INVOICE_SELLER_*; web CheckoutDialog (coupon column, breakdown, Pay with Razorpay via checkout.js, MockCheckout for the mock server), PaymentHistory + InvoiceDialog, CouponsAdmin; mock routes + `/__test/seed { coupons, payments }`. GAPS #49 (webhook secret + public URL). Note: my env check found RAZORPAY_KEY_ID / RAZORPAY_KEY_SECRET present but empty in the root .env at the time; the API answers 503 on checkout until they hold values. Verified: gate 30/30 (--concurrency=3), api 222 tests (branches 90.06 %), web 321 unit (functions 90.06 %), schema 176, Playwright 59/59 with 6 new captures, fact-checker clean. Next: /commit → PR #18, then 4c promotional emails, then item 5 (public / legal / share pages, assistant, tour).
- 08 Sep (late night, `feat/admin-banners`, uncommitted): Phase 4 item 4b (ADR-033, docs/design/admin.md §4b): `banners` table with bytea images (migration 0005), `banners.ts` + `routes/banners.ts` (trader list of showing banners, image endpoint with cache headers, admin CRUD with data-URL uploads under a 7 MB body limit), web BannersAdmin (filters, thumbnails, switch, edit, delete, preview as user), FlyerPopup carousel on /analyse with frequency rules in browser storage, palette 'Show announcements', mock routes + `/__test/seed { banners: n }`. GAPS #48 (no paging / object store yet). Verified: gate 30/30 (--concurrency=3), api 211 tests (branches 90.25 %), web 310 unit (functions 90.06 %), schema 174, Playwright 55/55 with 6 new captures, fact-checker clean. Next: 4c promotional emails.
- 08 Sep (late night, `feat/admin-console`, uncommitted): Phase 4 item 4a (ADR-032, docs/design/admin.md): User Management page (UsersAdmin + UserDrawer + UserDialogs + table-tools), API `admin-users.ts` + `routes/admin-users.ts` (detail, comped set-plan single/bulk, invite by OTP mail via `Mailer.sendInvite`, role / own-account / last-admin rules in PATCH), list gains plan/sort/dir and mobile / referralCode / paidInr / lastLoginAt columns, palette Admin commands, settings menu Admin section, `adminCols` in the ui store, mock mirrors all of it (per-account history, invites). GAPS #47 (no impersonation). Verified: gate 30/30 (--concurrency=3), api 209 tests (branches 90.05 %), web 300 unit above floors, schema 172, Playwright 51 (+ visual re-run 12/12 after fixing which row the drawer shot opens), fact-checker clean after correcting the spec-evidence row offset (the feature JSON index is id − 1; the first 4a pass used the id as the index). Next: 4b banners + flyer popup, 4c promotional emails.
- 08 Sep (late night, PR #15 merged 9579c25): Phase 4 item 3 (ADR-031, docs/design/billing.md 'Referrals and commissions'): `referral_commissions` table (migration 0004) written by `recordReferralCommission` on every subscription write (activation now; Razorpay checkout must call it too); `GET /v1/referrals`; admin commissions list / detail / mark (Paid or Not Paid with reason + proof; 409 when nothing pending) / bulk-pay; web `/referrals` page (ReferralsPage + ReferralBits: link card, code chip, tiles, hatched-bar chart, filters, sort, 10/page, CSV, Share dialog), admin Commissions tab (CommissionsAdmin: tiles, month filter, View, Mark Payment, select rows then Mark paid, Bulk pay confirm, chart), palette 'Referrals: copy link'; mock API mirrors the routes, unique referral code per mock account, `/__test/seed { referrals: n }`. GAPS #46 (frozen percentage). Verified: gate 30/30, api 203 (branches 90.12 %), web 292, schema 170, Playwright 45 + visual 6 with 8 new captures, fact-checker clean.
- 08 Sep (night, `feat/billing-plans`, uncommitted): Phase 4 item 1 (ADR-030, docs/design/billing.md): plan catalogue + menu pricing + entitlements (`entitlements.ts`, 403 UPGRADE_REQUIRED at paper start / live place / batch, ACCOUNT_DEACTIVATED), `GET /v1/subscription`, ₹0 activation (paid → 402 until Razorpay), admin plans / menu items / user subscriptions with bulk and audit; web My Subscription page, Upgrade Required dialog, admin shell + three pages + placeholders for later items; migration 0003; `PlanState` moved into the schema package; harness signs test users up on Elite. Razorpay test keys still needed from the user before item 2. Verified: root gate 30/30 (run with --concurrency=3 after an out-of-memory run at the default concurrency while the fact-checker and dev servers were up), api 198 tests, web 285 unit, schema 166, Playwright 43/43 (new account.spec.ts, admin.spec.ts), fact-checker 9/9. Lesson: never run the forced gate while a fact-checker subagent runs vitest; 16 GB is not enough for both plus three dev servers.
- 08 Sep (night, `feat/trading-closeout`, uncommitted): Phase 3 close-out (ADR-029): exchange margin in use in the live preview (Delta has no pre-trade estimate; docs checked), background reconciler `live-reconcile.ts` + `TRADING_RECONCILE_MS` + Live tab polling while pending, browser daily P&L writer `usePnlWriter`. GAPS #38, #40 closed, #34 partly. Harness exposes `deps`. Verified: root gate 30/30, Playwright 39/39, fact-checker 7/7.
- 08 Sep (late): `fix/currency-toggle-wallet` (uncommitted): header currency toggle sent a partial settings body (400, GAPS #45) → full object + error toast; wallet chip shows "reconnect key" and opens API Settings on the decrypt 409, and the API Settings dialog warns in place (closes GAPS #42's open item). The user still has to re-enter the testnet key once. Currency scope decision pending: keep USD + INR (reference parity) or add a currency list.
- 08 Sep (late): PR #10 `feat/builder-parity` merged (e799b75). Follow-up on `fix/lots-default-migration` (uncommitted): browsers that had persisted `chainLots: 10` under the old default kept 10 after the merge (the user saw it); the store now carries `lotsDefault` and lifts persisted lots to the new default once, later choices stick. Legs already in the Builder keep their own lots by design.
- 08 Sep (late night, `feat/builder-parity`, uncommitted): Builder parity with the reference site's current legs table (ADR-028): leg checkbox (`enabled`, `activeLegs`), in-place CE / PE, strike and expiry editors (`LegInstrument`, `setLegInstrument`, `useExpiries`), default 100 lots; payoff −2σ..+2σ labels, break-even %, target date; header wallet balance + USD / INR toggle. HC-TR-146 / 147. The branch also carries the uncommitted e2e fix from `fix/e2e-strip-tab` (one line + screenshots). Verified: root gate 30/30, web 264 unit, Playwright 39/39 (three expectations updated for the 100-lot default and the break-even %, plus GAPS #43: stale shared positions query after Go live, fixed), fact-checker 9/9.
- 08 Sep (night): testnet trial with the user found two real bugs and both are fixed on `fix/api-list-contract` → `feat/workspace-follow`: `GET /v1/brokers` lacked the paginated envelope's `nextCursor` so the web showed "No exchanges configured" (GAPS #41); with `CREDENTIALS_ENC_KEY` unset the API minted a vault key per process, so every tsx-watch restart made the saved exchange key undecryptable and the live preview said "Could not read the exchange wallet" (GAPS #42: dev key now derived from `BETTER_AUTH_SECRET`, undecryptable credential → 409 "reconnect", preview logs and shows the reason). Then the UX work the user asked for (ADR-026, ADR-027): the analysis pane follows the selected paper / live card (auto-follow, source bar with Back to Builder, `useStrategyAnalysis(scope)`), Live tab net-positions table from the venue with tick-to-analyse and Exit / Exit all (`POST /v1/strategies/live/positions/exit`, reduce-only, matching legs squared off, HC-TR-143..145), and a one-click templates strip under the Builder legs (`TemplatesStrip`, `useTemplateLoader`). Tests: api 187 (coverage 97.7 / 90.2 / 98.5), web 259 unit (positions.test.ts, pane-follow.test.tsx, builder strip test), root forced gate 30/30. Playwright NOT yet re-run for this branch (the user's dev server on :3000 blocks the e2e `next dev` on :3100); e2e specs and visual captures updated and pending a run.
- 08 Sep (evening): Phase 3 item 2, live trading (ADR-025): `packages/venues/src/delta/trading.ts` (signing, `contractsFor`, `DeltaTradingClientImpl` with token bucket / 5 s timeout / test-env guard / `unknown` outcome, `FakeDeltaTradingClient` incl. `forget`), schema `LivePreview` / `LivePlaceBody` / `LiveBatchBody` / `StrategyOrder`, API config `DELTA_TRADING_REST_URL` + `TRADING_*` limits, `strategy_orders` table + `users.trading_disabled` (migration 0002), routes `live/preview | place | retry | sync`, `live/batch`, `live/positions`, admin trading switch (`routes/live.ts`, `live-exec.ts`); repo-root `.env` loader (`env-file.ts`) in api + gateway; web `lib/api/live.ts`, TradeModeDialog `lockLive`, TradeFlow live path (server draft → preview → place with expected marks), TradePreviewDialog venue table, PaperPanel `kind="live"` (order chips, failed banner + Retry, Sync, exchange chip, Square off all), BatchLiveDialog, ConfirmAdjustmentDialog, Workspace Live tab + pulsing count. Batch repeat is idempotent on `strategies.order_batch_id`. Tests: venues 108 (trading.test.ts 7 + trading-more.test.ts 7; package keeps its 100 % line / function floor); gateway 63 + env-file.test.ts; api 182 (coverage 97.7 / 90.6 / 98.8 vs 95 / 90 / 95 floors; live.test.ts 10, live-edge 6, live-cover 4, db schema relationships); web 249 unit (trading-live.test.tsx 7, live.test.tsx 2; coverage functions 90.6 % vs 90 floor); e2e 39/39 incl. HC-TR-063 + visual analyse-live-preview / analyse-live; root forced gate 30/30; fact-checker 12/12 verified. Docs: trading.md as-built item 2, GAPS #37 closed, #40 opened, 11 spec rows carry item 2 evidence, env.example documents the testnet URL and limits. The user's dev server on :3000 (PID 9916) was stopped so Playwright could start `next dev` on :3100.
- 08 Sep (afternoon): Phase 3 item 1, strategies API + paper trading (ADR-024; docs/design/trading.md): `packages/schema/src/strategies.ts` contract; API tables `strategies` / `strategy_legs` / `strategy_pnl` (migration 0001), routes `/v1/strategies` list / create / get / patch / delete / start / legs / legs/:id/close / close / stop / archive / restore / pnl with audit rows, `errors.badRequest` / `errors.conflict` + 409 responses; web `lib/api/strategies.ts` (TanStack), `lib/strategy/{paper,usePaper}.ts` (fees, P&L at live marks per asset), trading components (TradeModeDialog, TradePreviewDialog, TradeFlow with the one-time legacy-draft import, PaperPanel, StrategyDetailsDialog, SquareOffDialog, PartialExitDialog, StopPaperDialog), Builder Paper trade / Live trade buttons, My templates from the API with Activate, ChainPickerDialog `onAdd` mode for adjustments, Workspace Paper tab with count pill; mock API mirrors the routes. Live trading stays disabled with a note (GAPS #37); daily P&L points have no writer yet (GAPS #38). Tests: schema 5, api strategies.test.ts 6 (api 159 total, coverage functions 95.3 % against the strict 95 % floor), web 240 unit (paper.test.ts, trading.test.tsx, trading-more.test.tsx, strategies.test.ts; builder / workspace / store updated; coverage functions 90.1 % just above the floor), e2e HC-TR-022 block + visual analyse-trade-mode / analyse-paper / analyse-details. Spec: 48 rows carry item 1 evidence.
- 08 Sep (morning): Phase 2 close-out on `chore/phase2-closeout`: settings dialogs audited against the v2 mock (all seven match; spec rows HC-SH-027..049 and the other Phase 1 rows now cite their unit / e2e tests), ‹ › expiry-strip arrows on overflow (ExpiryStrip.tsx, chain + picker, HC-WS-008 / HC-TR-028), chain pill Live / Connecting / Stale / Paused (HC-WS-009), spot-zone readout in the payoff strip (HC-WS-046), Builder price flash (HC-TR-012), GAPS #30 minors closed (plan ordering, PGlite out of the API image, `.npmrc` in the build, `/metrics` token + per-IP cap, migration advisory lock, compose digest pin, XAUT spot verified live). Dev servers started for the user's manual testing (web 3000, api 3001 on PGlite, gateway 3002, compose Postgres / Redis up).
- 08 Sep (early): Phase 2 item 4, Builder + templates + analysis pane (ADR-023; docs/design/workspace-builder.md "As built"): BuilderPanel (Side · Instrument · Lots · IV · Price · Δ · Θ/d · ✕, live / custom prices, basket, New / Clear, ticket, Save draft / Update / Save & trade, 8-leg rule), TemplatesPanel (28 templates placed on the venue ladder, My templates drafts | archived with search / Load / Archive / Delete), ChainPickerDialog, FutureDialog, SaveDraftDialog; AnalysisPane with PayoffPanel (tiles, canvas chart via payoffDraw.ts, layers, zoom, target sliders, greeks strip), GreeksPanel, LadderPanel, Phase 5 placeholders; Workspace rewrite (tabs from the store with the Builder count pill, 35–70 % divider, stacking < 1000 px); pricing in a Web Worker (lib/pricing), money helper USD / INR, palette commands, expiry chip leg dot; expiries drop at their settlement instant. Fixes on the way: Turbopack alias for @hapiecoin/pricing (GAPS #36), chart box height, ResizeObserver guard. Root gate `pnpm turbo run typecheck lint test:coverage build --force` 30/30 (web 224 unit, coverage functions ≈ 91 % against the 90 % floor, web `testTimeout` raised to 20 s because the forced gate loads the laptop), e2e 37/37 then 18/18 after the expiry / chart fixes (analyse.spec HC-TR-040/009/027 blocks, visual captures analyse-builder / analyse-templates / analyse-greeks), fact-checker 14/14 RIGHT. Spec: 78 rows carry item 4 evidence. GAPS #3 closed, #33–#36 opened.
- 07 Sep (late night): Phase 2 item 3, row controls and legs (ADR-022; design § Item 3): per-asset persisted legs in the UI store (limit 10, normalised on read), hover / highlight control [B][S][− lots +][ⓘ] per side, add at mark with persisted `chainLots` presets, pills under the strike + mark outline + edge stripes + filled B/S, keys B/S/Shift+B/Shift+S/Enter/Esc/+/−, Option details dialog (Delta symbol, live figures, Buy/Sell + lots). Sparkline deferred (GAPS #32). Files: lib/strategy/legs.ts, components/chain/RowControls.tsx, dialogs/OptionDetailsDialog.tsx. Web 190 unit; e2e HC-WS-023/024/026/027 + visual analyse-legs. Committed as 74d5b82 and pushed on feat/chain-row-controls; PR: https://github.com/subashkone/hapiecoin/pull/new/feat/chain-row-controls.
- 07 Sep (night): Phase 2 item 2, chain columns (ADR-021; design § Item 2 in docs/design/options-chain.md): 13 optional columns (prices+IV, OI, Δ Γ Θ ν, volume, bid/ask qty, 24h change, last) laid out from the strike outward and mirrored; persisted `chainColumns {v:2, order, visible}` with normalisation; Column Settings dialog (gear in the chain tools; Show/Hide switches with counter, presets Essentials/Show all/Hide all/Reset, Greeks trio toggle, Reorder tab with ▲▼ + drag, OHLC muted); palette "Chain: column settings" / "Chain: toggle Greeks columns". OHLC deferred (GAPS #31). Files: components/chain/columns.ts, lib/chain/layout.ts, dialogs/ColumnSettingsDialog.tsx. Web 176 unit; e2e HC-WS-010/012/013/014 in analyse.spec.ts. Merged to main via PR #3 (9fbb630).
- 07 Sep (evening): Phase 2 item 1, the options chain layout (docs/design/options-chain.md, ADR-020): fixed centre strike column, one virtualiser driving calls / strike / puts row lists, mirrored horizontal offset (wheel, ← →, detached scrollbars), pinned two-row header, ±6/±12/all range control (persisted `chainRange`), OI inward bars, ITM tint, ATM band + auto-centre, J/K/↑/↓/Home/End/A/E keys, palette commands, single-side view under 560 px, stale "as of" footer. Files: apps/web/src/components/chain/{ChainTable,ChainHeader,ChainTools,columns,useMirroredScroll}, lib/chain/range.ts. GAPS #1, #2 closed; HC-WS-015..020, 022 carry evidence in the spec. Gate 30/30; web 163 unit + 30 e2e. Uncommitted on feat/phase1-foundation (commit with /commit when asked).
- 07 Sep (later): Docker fixed by installing Windows 11 25H2; compose Postgres/Redis verified with the API and gateway; `hapiecoin-api` and `hapiecoin-gateway` images build and run (GAPS #17, #22 closed). Phase 1 `/review` (code-reviewer + fact-checker): 1 blocker, 6 majors, 8 minors → GAPS #23–#30; all but the minor batch fixed the same day under ADR-019 (trusted-proxy client IP, `BETTER_AUTH_URL` = web origin, production requires DATABASE_URL + RESEND_API_KEY, shared referral schema, feed re-watch after refresh, spec rows HC-WS-107/108 + HC-PB-065). Gate after fixes: 30/30 tasks, api 153 / gateway 59 (+1 skipped) / web 144 unit + 27 e2e.
- 07 Sep: Phase 1 delivered. Monorepo (pnpm 12 + Turborepo 2, TS 6.0.3, ESLint 10, Vitest 5). Packages: `schema` (154 tests), `pricing` (83, validated against 307 live Delta tickers, ADR-012), `venues` (94, live Delta REST+WS proven), `ui` (80, tokens generated from the v2 mock). Apps: `api` (Hono + Better Auth email OTP/Google/passkeys, Drizzle on PGlite/Postgres, vault, security headers, 149 tests), `gateway` (ws transport, coalesced deltas, 58 tests, live chain proven), `web` (Next 16, landing/auth/legal/analyse shell with live chain, 144 unit + 26 e2e, 327 KB gz on /analyse). Root gate `pnpm turbo run typecheck lint test:coverage build`: 30/30 tasks green.
- 07 Sep: real integration run web→api→gateway→Delta: sign-up, OTP from API log, verify, `/analyse` shows FEED LIVE with the nearest expiry's real strikes; session valid. Fixes found by it: web auth rewrite now targets `/v1/auth` (ADR-018), gateway CORS on `/healthz`, favicon.
- 07 Sep: product renamed to HapieCoin in every file except the reference clone and the guard/rule files; v2 mock, inventory and spec republished; ADR-011..017 and GAPS #7–21 recorded by the build agents.

## Blocked by
- Nothing. Docker fixed 07 Sep 2026 by installing Windows 11 25H2 (GAPS #22 closed); compose Postgres + Redis verified with the API and gateway the same day (docs/CLAUDE-SETUP.md → "Docker on this machine").

## Next steps
0. **Adjustment workbench (user priority, ADR-044)**: A1 + A2 MERGED to main (PRs #27, #28, 09 Sep) → A3 `feat/adjust-live` BUILT (gate + Playwright + commit pending) → then Analyse tabs, then 5.5 Terminal (live confirm, plans compare, quick fixes + ranking, scenario date, alert stub). Each PR: unit at floors, Playwright alone, gate alone, fact-checker, graphify.
1. Phase 5 item 1 · Analyse extras that need no new data: Journal panel (10 rows), Scenarios / Vol / Structure analysis tabs and Payoff / Greeks / Ladder extras (24 rows), Strategy Details and trade-dialog extras (9 rows), Builder / Templates extras (18 rows). Branch `feat/analyse-extras`; one PR per tab group.
2. Phase 5 item 2 · Alerts: alerts bell + Alerts center (HC-SH-079, 7 rows) with price / IV-rank / strategy-P&L rules, delivery by push, email (Resend) and Telegram; evaluator in the API on the gateway feed. ADR for the rule model and delivery guarantees.
3. Phase 5 item 3 · Header market stats and chrome: ATM IV + IV rank, expected move per expiry (HC-SH-077/078), portfolio bar items, keyboard-shortcuts dialog, density polish, remaining palette commands.
4. Phase 5 item 4 · Share pages with OG images (2 rows) + assistant / tour extras (6 rows).
5. Phase 5 item 5 · Market Analytics: PRs 5.1 (#21), 5.2 (#22) merged, 5.3 (#23) merged; next 5.4 options (Deribit + Delta) / sentiment / whales (Hyperliquid) + coming-soon panels, 5.5 terminal; ingest Bybit fallbacks for long-short, liquidations and a tickers job (GAPS #57) so the user's Binance/OKX-blocked network fills the charts. Earlier note (resolved by ADR-038, hybrid): was BLOCKED on the user's data-source decision: which provider (Delta only, a market-data API with a key, or scraped public feeds), budget, and refresh cadence. Ask before designing.
6. Always: rebuild packages before Playwright after any rebase (`pnpm --filter @hapiecoin/schema --filter @hapiecoin/pricing --filter @hapiecoin/ui --filter @hapiecoin/venues build`); run the root gate alone (16 GB machine); GAPS #51 flaky e2e watch.
