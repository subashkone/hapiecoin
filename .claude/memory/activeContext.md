# Active context · HapieCoin

**Current phase**: Post-Phase-5 roadmap (docs/ROADMAP.md). Items 1–12, 17–20, 24–28 and the PWA half of 23 are MERGED (main 933dcdc, 13 Sep 2026); the gaps batch (GAPS #61, #90, #15, #94-security, #4, #35, #12) is MERGED (PRs #86–#96, ADR-081..089, GAPS to #105). Open on the build side: 21 E2 currency, 22 E3 languages, 23 Capacitor half (needs store accounts), 29 zod/mini + bundle check. Waiting on the user: 13 deployment (provider), 14 partnership, 15 pricing split, 16 paid data. Post-freeze launch defects (ADR-090 rule 5) go as fix branches: PR #98 typed-LIVE field (GAPS #107) and PR #99 short-leg margin estimate + buys before sells (ADR-091, HC-TR-192, GAPS #108/#110 fixed, #109 open) MERGED 15 Sep 2026 (main c3eb66a); PR #100 TESTNET tag (ADR-092, HC-SH-138, GAPS #111 fixed, #112 open) MERGED 16 Sep 2026 (main 619f81e): every live trade so far went to the Delta India testnet, now labelled. Numbering after 16 Sep: next free ADR-093, GAPS #113, HC-TR-193, HC-SH-139, migration 0024.

**Last updated**: 2026-09-16 (session 5890: TESTNET tag merged as PR #100, main 619f81e; the dev stack runs from the session with api+gateway and web under separate processes, ports 3000-3002).

## Recent

Trade lifecycle A3 + A4a + A4b (11 Sep 2026, session f51a): A3 expiry settlement job + close reasons MERGED as PR #62; A4a strategy stop / target rules run by the API + capital gate (HC-TR-165..168, GAPS #81 closed, #82 open) MERGED as PR #67 (main c57d257); A4b leg stop / spot level / time exit / workbench Protect entry / Re-enter (HC-TR-169..172, ADR-059 addendum, migration 0016 leg_id + scope; peer f9 regenerates theirs as 0017 if it lands second) BUILT on feat/trade-lifecycle-5, awaiting review + serial gate + Playwright + commit. Next: A5 accounts / sub-accounts, then B items; deployment waits on the provider choice.

A5 accounts (11 Sep 2026 late, session f51a): A4b MERGED as PR #69 (main a4b8dac; local dev DB fixed by hand after the 0016/0017 renumber; user's account comped to Pro and made admin for testing). A5 BUILT on feat/accounts (ADR-068; HC-SH-123, HC-TR-173..175; GAPS #85; migration 0018 label + account_id; numbering agreed with the E20 peer, which holds ADR-066/067 and HC-SH-121/122 on its unmerged branches): labelled keys per exchange in API Settings, the account select in the trade dialog, openCredential resolves the strategy's key (never guesses), one positions read per account on the Live tab, delete refused while a live strategy trades through the key. Awaiting review + serial gate + Playwright + commit. User confirmed Delta sub-accounts exist and created one on the demo account; key scoping to a sub-account still to be seen in practice (GAPS #85). 12 Sep: reviewer pass fixed (batch per-row key, restrict FK, two-key api suite), serial gate 34/34, Playwright 96, fact-checker clean; COMMITTED 3eba51e + merge of main e167af1 (PRs #70 pricing calendar, #71 Deribit) as ac565eb, PUSHED on feat/accounts, awaiting the user's merge and CI. Numbering settled with the E20 peer session: A5 = ADR-068 / HC-SH-123 / GAPS #85, its venue switch 4b = ADR-069 / HC-SH-124 / GAPS #86 (4b merges after A5 and regenerates the spec). NEXT after the merge: roadmap B7 verified P&L from Delta fills; deployment (C13) still waits on the user's hosting provider choice.

B7a verified P&L (12 Sep 2026, session f51a): A5 merged PR #72, its follow-ups (margin tile per account, Paper/Live/Journal per account) merged PR #75; Delta scoping of keys to sub-accounts verified on the demo account (GAPS #85). B7a BUILT on feat/verified-pnl (ADR-073; HC-TR-179..181; GAPS #89; migration 0019 venue_fills + fill_watermarks): listFills on the venue client (read-only), ingest job FILLS_INGEST_MS + Refresh route, verifiedFromFills arithmetic in the schema package, Journal block with the agreement line. Delta's fill shape unverified until the user's first Refresh (a key-capture script was refused, rightly). Awaiting review + gate + Playwright + commit. NEXT: B7b public page per trader + share to X / Telegram (decisions: handle format, what the page discloses). Push order: after the peer PRs #76 (spot per venue, ADR-071) and #77 (Strategy Wizard, ADR-072, HC-TR-176..178, GAPS #88) merge, merge origin/main into feat/verified-pnl, rebuild the spec (the three rows then read HC-TR-179..181), push.

B7b public page (12 Sep 2026, session f51a): B7a MERGED as PR #78 (main f828665). B7b BUILT on `feat/verified-public` from main f828665 (ADR-075; HC-SH-127, HC-PB-066, HC-PB-067; GAPS #91): `trader_pages` + `Handle` / `PublicPageSettings` / `publicTraderFrom` in the schema package, `GET/PUT /v1/public-page` and the public `GET /v1/public/traders/{handle}` (404 alike when unknown or off, max-age=60), `/t/<handle>` page with `TraderPage` + `ShareBar` (X intent, Telegram share, copy link, canvas PNG card), the Public page dialog under Account, mock routes + `publicHandle` seed. Decisions from the user: chosen handle, totals + trend always with daily / accounts / months opt-in, all three share targets. Migration generated as 0020 on the branch; the peer's Mindful pause (ADR-074, HC-TR-182..183) holds 0020 on its own branch, so whoever merges second regenerates (mine becomes 0021 if the peer lands first). Next free: ADR-076, GAPS #92, HC-TR-184, HC-SH-128, HC-PB-068.

B9a backtest (12 Sep 2026, session f51a): B7b MERGED as PR #81 (main 10fb851); items 5 and 7 ticked. B9a BUILT on `feat/backtest` from main 10fb851 (ADR-077; HC-SH-128, HC-TR-184..185; GAPS #93; migration 0022 chain_eod): `chain_eod` written once a day at the settlement hour by the snapshotter with a week backfilled from the mark stream (`apps/api/src/chain-eod.ts`), the template catalogue moved to `packages/pricing/src/templates.ts` (web re-exports), `runBacktest` in `packages/pricing/src/backtest.ts`, `GET /v1/backtest`, the Backtest tab + palette command, mock `test/mock-backtest.ts` with the `backtestDays` knob. Carries the peer's one-line fix of the HC-TR-169 time-exit test (a fixed past date) in strategies.test.ts. Next: B9b replay of an expiry's chain. Next free after this: ADR-078, GAPS #94, HC-TR-186, HC-SH-129, migration 0023.

B9b replay (12 Sep 2026, session f51a): B9a MERGED as PR #82 (main 2b58b31). B9b BUILT on `feat/replay` from main 2b58b31 (ADR-079; HC-WS-113..114; GAPS #95; no migration): `apps/api/src/replay.ts` + `routes/replay.ts` (expiries / steps / chain), `packages/schema/src/replay.ts`, `ReplayPanel.tsx` + `lib/api/replay.ts`, the tab in the pane and the palette, mock `test/mock-replay.ts` with the `replayDays` knob. The peer's item 28 (PR #83, ADR-078, GAPS #94, migration 0023) touches store.ts, CommandPalette.tsx, mock-api.ts and analyse.spec.ts too: merge main before pushing. Next free after this: ADR-080, GAPS #96, HC-WS-115, HC-TR-187, HC-SH-130, migration 0024.

Phone pass (12 Sep 2026, session f51a): B9b MERGED as PR #84 (main 62bfb2b); item 9 complete. Item 12 BUILT on `feat/phone-pass` from main 62bfb2b (ADR-080; HC-SH-130..131, HC-WS-115; GAPS #96; no migration): scrolling tab strips (packages/ui Tabs), `lib/useMediaQuery.ts`, viewport export, compact header below 768, chain touch pan + tap-to-act with 36 px controls, tables scroll in their container, a `phone` Playwright project (Pixel 7) running `e2e/phone.spec.ts` only. The peer's item 28 (PR #83, ADR-078, GAPS #94, migration 0023) touches AppHeader.tsx (menu-security after menu-mindful) and PaperPanel.tsx (RetryDialog): keep their blocks when merging main. Next free after this: ADR-081, GAPS #97, HC-SH-132, HC-WS-116, HC-TR-187, migration 0024.

Home-screen app (13 Sep 2026, session f51a): item 12 MERGED as PR #85 (main 468acf2). Item 23 PWA half BUILT on `feat/pwa` from main 468acf2 (ADR-082; HC-SH-134..135; GAPS #98; GAPS #68 closed; no migration): `app/manifest.ts`, `public/icons`, `public/sw.js` (deny-by-default, unit-tested in a sandbox), `app/offline`, `lib/pwa/install.ts`, `shell/Pwa.tsx` mounted from the root layout (not Providers.tsx, which the peer's item 27 edits), `dialogs/InstallDialog.tsx`, `menu-install`, CSP worker-src / manifest-src, a `phone-ios` Playwright project (iPhone 14 on Chromium). The peer's item 27 (feat/observability, ADR-081, GAPS #97, HC-SH-132/133) is committed and awaits push; next free after both: ADR-083, GAPS #99, HC-SH-136, HC-WS-116, HC-TR-187, migration 0024.

Go-live pass (13 Sep 2026, session f51a, branch chore/go-live-docs from main 933dcdc): feature freeze (ADR-090). Gate 34/34 alone; Playwright desktop 109 passed (9.6 min, traces on), phone + phone-ios 10 passed, guide captures 8 passed, analytics rerun 10 passed. One defect found and fixed: GAPS #106 hydration mismatch on /analytics/markets, /derivatives, /options (Suspense boundaries removed; regression test in analytics.spec). Written: docs/BACKLOG.md (56 open gaps, 8 roadmap items, 62 mock-only rows), docs/GO-LIVE-READINESS.md (verdict, verification table, unverified list, risks by severity, launch checklist), docs/guide/README.md + 14 area pages generated from the spec with 165 captures (gen-guide.js in the session scratchpad; captures from visual.spec + the new e2e/guide.spec.ts), scripts test:e2e:ui / headed / report; ROADMAP items 21, 22, 29 marked backlog, 23 done. Pre-deploy engineering still open: apps/web and apps/ingest Dockerfiles, a compose manifest, secrets; user decisions: provider, DNS, Resend, Razorpay live + webhook, Telegram token.

Launch defects (15 Sep 2026, session 5890): the user could not tell why Place live orders stayed disabled (the LIVE field's placeholder was the word itself) → PR #98 fix/live-confirm-hint (blank placeholder, red border + hint until typed, green + tick on match; GAPS #107; ADR-078 unchanged). A live condor then lost both shorts to insufficient_margin while the longs filled → PR #99 fix/short-margin-check (ADR-091, HC-TR-192): shortOptionMarginUsd in packages/venues from Delta's initial_margin / scaling factor + ticker spot_price, LivePreview.marginRequired refuses place / Trade All / adjustment batch / add-legs before any order, refusal by name when one short could not be priced, placeEntries + retryFailed send buys before sells, dialogs show "Needed for shorts · est."; GAPS #108 fixed, #109 open (scaling-factor unit unverified), #110 replay fixture clock window fixed. Verified: venues 156 / schema 220 / api 404 / web 751 unit, Playwright live + batch + replay + /analyse visual, code-reviewer 2 rounds, fact-checker.

## Blocked by
- Memory on the dev laptop (15 GB): with Docker + WSL (~4.8 GB), SnippingTool (2.5 GB when a capture is left open) and several Claude sessions, the web production build and the full vitest run die of commit exhaustion. Run the gate alone, close SnippingTool, run vitest with --maxWorkers=2 when it happens. Not a code defect (GAPS #78 pattern).
- Deployment (item 13) waits on the user's hosting provider choice; the Capacitor half of item 23 waits on Play Store / App Store accounts.

## Next steps
1. Decide with the user when to point `DELTA_TRADING_REST_URL` at the real exchange with real keys (all live trades so far were testnet, see ADR-092); then a small first real trade to close GAPS #109 (compare "Needed for shorts · est." with Margin in use). Optional before that: GAPS #112 (testnet-aware wording / greyed strikes). Dev stack after a restart: `pnpm db:up`, rebuild schema/pricing/venues/ui, api+gateway and web as separate processes (stopping the web task under one turbo kills the others).
2. Next item (proposed, awaiting "go"): 21a currency data side (ECB rates ingest job on the ADR-062 runner, `fx_rates` table + migration 0024, `GET /fx`, manual override with a basis badge in settings) then 21b display side (Money type in packages/pricing, currency list, one formatting path). Alternative: item 29 zod/mini + bundle check (small).
3. On that branch also flip ROADMAP item 23 to "PWA half ✅ (PR #95) · Capacitor half waits on store accounts".
4. Always: plan → build → unit at floors → Playwright alone (dev stack stopped) → gate alone → code-reviewer + fact-checker → ADR + GAPS + feature rows + buildspec → /commit → merge main before push (append-only conflicts: main's block first, regenerate the spec, check positional HC ids against test titles) → push → user merges.
