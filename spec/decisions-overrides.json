{
  "HC-TR-089": { "status": "confirmed", "acceptance": "'Trade All → Live' opens a batch selector listing every open paper strategy with a checkbox (all ticked by default), a per-strategy margin check and a single Trade Preview of all selected legs. Nothing converts without an explicit tick and a final confirmation; strategies failing the margin check are shown blocked, not skipped silently. Result toast lists converted and blocked counts." },
  "HC-TR-025": { "status": "confirmed", "acceptance": "Builder state is kept per asset. Switching BTC ↔ ETH ↔ XAUT never clears legs: the header shows a count badge per asset and the builder shows that asset's legs (empty if none). No confirmation dialog is needed because nothing is lost." },
  "HC-TR-047": { "status": "confirmed", "acceptance": "Drafts can be archived from the list; the toast offers Undo for 8 seconds; archived strategies list a Restore action. Archive is a soft state, never a delete." },
  "HC-TR-078": { "status": "confirmed", "acceptance": "Details of a draft show 'Load in Builder', 'Activate' and 'Duplicate'; details of an archived strategy show 'Load in Builder', 'Restore' and 'Duplicate'; performance is labelled 'Final' only for archived strategies and shows realised P&L, days tracked and the equity curve." },
  "HC-AD-049": { "status": "confirmed", "acceptance": "Per-user lot sizes are edited inside the user drawer's Limits tab (not a separate icon), validated as > 0, written to the audit log with before/after values, and take effect on the user's next chain load." },
  "HC-AD-058": { "status": "confirmed", "acceptance": "'Bulk pay' requires a payment reference (UTR / transaction id) and shows the total and count before confirming; on confirm every pending commission is marked paid with that reference, a CSV of the batch is offered, and the action is audit-logged. Individual 'Mark Paid' remains for exceptions." },
  "HC-AD-090": { "status": "confirmed", "acceptance": "Row click opens the quick-view drawer (profile, subscription, referrals, limits, lot sizes); the drawer header links to a full page /admin/users/:id for deep edits and history. Esc, scrim and ✕ close the drawer; focus is trapped while open." },
  "HC-SH-074": { "status": "confirmed", "acceptance": "Tour step 12 text: 'Stop a paper trade — Use Stop when you are done. Choose whether to archive it so the result stays in your journal.' Steps 10–12 switch the left tab to Paper automatically." },
  "HC-AC-055": { "status": "confirmed", "acceptance": "If referrals fail to load, the page shows an inline error panel 'Couldn't load referrals' with a Retry button and a toast; the last successfully loaded data stays visible with a 'stale' note when available." },
  "HC-AD-067": { "status": "confirmed", "acceptance": "Production requires an uploaded image (PNG/JPG/WebP, max 5 MB, recommended 1200×630); the form shows a live preview and a size/format validation message. No gradient fallback in the real product." },
  "HC-AD-070": { "status": "confirmed", "acceptance": "Banners page has an error state 'Couldn't load banners' with Retry; when a cached list exists it is shown with a 'showing last loaded data' notice." }
}
