# HapieCoin · gap backlog (from review of the mocks)

Items found while reviewing the clone and v2. Each becomes a build/test item in the real implementation.

| # | Area | Gap | Source | Status |
|---|------|-----|--------|--------|
| 1 | Options chain (v2) | v2 shows only strikes at the mock 500-step; the original lists every listed strike. Real Delta chains mix steps (denser near ATM, wider far out) and the number of strikes differs per expiry. Fix: drive strikes from the instrument list per expiry, never from a fixed step. | user review, 06 Sep | closed 07 Sep 2026 (Phase 2 item 1): rows are exactly the gateway snapshot, the ±6/±12/all control only slices them; unit HC-WS-108 + range.test.ts, e2e HC-WS-108/HC-WS-016 in analyse.spec.ts (ADR-020) |
| 2 | Options chain (both) | Calls and Puts must scroll together: vertical scroll is shared (one row = one strike), horizontal scroll on one side must move the other side the same amount, with the Strike column fixed in the centre. | user review, 06 Sep | closed 07 Sep 2026 (Phase 2 item 1): one virtualiser drives three aligned row lists around a fixed 92 px strike column; horizontal offset mirrored between calls and puts via wheel, keys and detached scrollbars; unit GAPS-2 in chain.test.tsx, e2e GAPS-2 in analyse.spec.ts (ADR-020) |
| 3 | Builder | Keep builder legs per asset (BTC/ETH/XAUT) with a count badge; switching assets must not clear anything. | ADR-010 (HC-TR-025) | closed 08 Sep 2026: legs, strategy meta and the Builder count pill are per asset in the UI store (ADR-022, ADR-023); `setAsset` only resets the payoff target; store.test.ts + workspace.test.tsx HC-WS-005 |
| 4 | Trading | "Trade All → Live" as an explicit batch selector with per-strategy margin checks and one combined preview. | ADR-010 (HC-TR-089) | open |
| 5 | Admin | Full user page `/admin/users/:id` (deep edits, history) in addition to the quick-view drawer. | ADR-010 (HC-AD-090) | open |
| 6 | Admin | Bulk commission pay requires a payment reference and exports the batch CSV; audit-logged. | ADR-010 (HC-AD-058) | open |
| 7 | Fixtures | `spec/fixtures/delta-tickers.json` holds BTC call tickers only; put-side Greeks and exchange put-call parity are untested against Delta. Refresh the fixture with `P-BTC-*` tickers (and ETH/XAUT) and re-run `packages/pricing` validation. | pricing agent, 07 Sep | open |
| 8 | Venues / schema | Venue and schema packages named Instrument/Quote fields differently; bridged by `packages/venues/src/schema-adapter.ts` (ADR-014). Consider a single vocabulary in Phase 2 to remove the adapter. | venues agent, 07 Sep | open · since ADR-063 the bridge is the adapter's `schema` slot and the registry is keyed by the schema id, so the two vocabularies meet in one place |
| 9 | Live data | Delta's compact `ticker` channel omits `oi` in underlying units and `volume`; if the migration away from legacy `v2/ticker` happens, OI/volume must come from REST polling. | venues agent, 07 Sep | open |
| 10 | API / security | `CREDENTIALS_ENC_KEY` has no key id or rotation path: rotating it makes stored credentials undecryptable until users reconnect. Add a key version column and a re-seal job before launch (spec: quarterly rotation, KMS-held key). | api agent, 07 Sep | closed · ADR-054 (11 Sep 2026): key ids on rows, `CREDENTIALS_ENC_KEYS_PREVIOUS` keyring, lazy re-seal on use + `pnpm db:reseal`; KMS-held key remains an ops choice |
| 11 | API / venues | Delta private signing (`HMAC_SHA256(secret, method + timestamp + path + query + body)`, headers `api-key`/`timestamp`/`signature`) is implemented from the docs and unit-tested, but has not been exercised against the live venue with a real key; verify once a whitelisted test key exists. | api agent, 07 Sep | open |
| 12 | API / auth | Passkey registration/authentication is configured (`@better-auth/passkey`) but not covered by tests: WebAuthn needs a browser authenticator. Cover in Playwright with a virtual authenticator in Phase 2. | api agent, 07 Sep | open |
| 13 | API / infra | `RedisRateStore` is tested against an in-process fake of the sorted-set commands; run it against the Docker Redis (`pnpm db:up`) once the machine restart lands. Testing rule says API handlers run against real Postgres: today they run on PGlite (ADR-013); add the Testcontainers/Docker job in CI. | api agent, 07 Sep | open |
| 14 | Gateway | `fut:<venue>:<symbol>` topics validate in the schema but the gateway answers `err unsupported_topic`: the Phase 1 feed loads options only (spot comes from the perpetual's `spot_price`). Serve futures tickers with a `fut` frame in Phase 2 (HC-PB-009 landing tiles need BTCUSD/ETHUSD/XAUTUSD). | gateway agent, 07 Sep | open |
| 15 | Gateway / schema | `Quote.oi` is required in the schema but Delta's compact `ticker` channel and some frames omit `oi_contracts`; the adapter emits `"0"` then, which is indistinguishable from a real zero. Make `oi` optional in the schema or derive contracts from `oi / contract_value`. | gateway agent, 07 Sep | open |
| 16 | Gateway / infra | `RedisPubSub` fans out per process: two gateway processes on one Redis each run their own Delta feed and publish every frame twice. Add a feed-leader (Redis lock) or a separate feed process before scaling past one gateway. Redis integration test runs only when `REDIS_URL` is set. | gateway agent, 07 Sep | closed 11 Sep 2026 (ADR-062): one gateway holds the feed lease (`hapiecoin:leader:gateway-feed`) and runs the venue socket; the others follow: their clients' topics reach the leader through the held-topic registry, first frames come from the snapshot store the leader writes, live frames through the fan-out; takeover within `LEADER_TTL_MS`. Tests: leader.test.ts, coordination.test.ts, role.test.ts, fanout.test.ts (two gateways on a fake Redis). A real-Redis two-process run is still to do with deployment |
| 17 | Gateway / infra | `apps/gateway/Dockerfile` (node:24-alpine, `pnpm deploy --legacy`) has not been built: Docker on the dev machine needs a restart. Build and run it once, then wire the compose service. | gateway agent, 07 Sep | closed 07 Sep 2026: both images build from the repo root (`hapiecoin-gateway` 247 MB, `hapiecoin-api` 403 MB) and were smoke-tested; fixes: root `.dockerignore` (host node_modules were overwriting the container install), pnpm 12 flags in `apps/api/Dockerfile` (`--prod=false` removed, `deploy` on the single project with `--legacy`). Compose services for the apps themselves are still to be added. |
| 18 | Web / ui | `@hapiecoin/ui` has no `"use client"` directives and creates contexts at import; server components cannot import it, so `apps/web/src/components/ui.ts` re-exports the parts it needs behind one boundary. Add the directive to the package entry points (or per component) and drop the boundary file. | web agent, 07 Sep | open |
| 19 | Web / bundle | The largest first-load chunk on `/analyse` (~88 KB gzipped) is `zod` 4 classic pulled in by `@hapiecoin/schema` for WS-frame and settings validation; first-load is 326.7 KB gzipped against a 350 KB budget. Move the schema package to `zod/mini` (tree-shakeable) or ship a lighter validator for the hot WS path before Phase 2 adds the builder and charts. | web agent, 07 Sep | open · 11 Sep 2026: 382.8 KB gz over 23 first-load chunks after PRs #48-#52 (check-budget reports 0 on-demand chunks, so the workspace split is no longer detected or no longer applies); ci.yml runs check:budget with continue-on-error so it shows as a warning, not a failure, until roadmap F29 (zod/mini, re-split) brings it under 350 KB |
| 20 | Web / e2e | Playwright runs against an in-memory mock API (`apps/web/test/mock-api.ts`) and a fake gateway (`test/fake-gateway.ts`) that mirror the landed contracts; the real web ↔ api ↔ gateway integration run has not happened yet. Screenshots under `apps/web/e2e/__screenshots__/` are captured, not pixel-diffed against `mockup-v2/shots`; the tolerance gate is still to be set. | web agent, 07 Sep | partly closed · ADR-055 (11 Sep 2026): `pnpm --filter @hapiecoin/web test:smoke` runs the web app against the real API + gateway (sign-up OTP, live chain, paper trade, alert, reload, sign-out); the screenshot pixel-tolerance gate is still open |
| 21 | Web / product | Wallet balance chip (HC-SH-008) and the "Wallet:" line in the API dialog are placeholders ("—", arrives with live trading); "Continue with Google" renders only when `NEXT_PUBLIC_GOOGLE_ENABLED=true` and is not covered end-to-end; `/auth/delta` explains rather than signs in (Phase 1). | web agent, 07 Sep | open |
| 22 | Infra / dev machine | Docker Desktop cannot start on the dev laptop: WSL 2 VMs boot but Linux init cannot reach the host over the Hyper-V socket (`UtilConnectVsock connect port 50000 failed 110`, `CreateVm/0x800705b4`), so `pnpm db:up` and the Dockerfiles are unverified here. Verified instead on 07 Sep 2026 with portable PostgreSQL 17.9 + Redis 8.10.1 binaries (migrations, OTP sign-in, sessions, Redis rate limits, Redis pub/sub chain fan-out all pass). Second pass 07 Sep 2026 (ETW): host WSL service binds port 50000 correctly and the host HvSocket layer never receives the guest connect; WSL 2.6.3, kernel/config variants, DISM+SFC all ruled out; no McAfee/AMD errors logged. Ryzen Master driver disabled + reboot: no change (reverted). vmbusr trace: the guest's connect message never reaches the host VMBus driver. McAfee removed + reboot: no change. Root-cause lead: OS stuck on build 26100.6584 (Sept 2025 level), every cumulative/25H2 install since April 2026 failed on delivery (0x80240034/0x80246007/0x80242016). Next: install the pending 25H2 (KB5121003) from Settings > Windows Update, reboot, retry; then in-place repair if still failing. See docs/CLAUDE-SETUP.md "Docker on this machine". | Phase 1 verification | closed 07 Sep 2026: fixed by installing Windows 11 25H2 (host Hyper-V/VMBus stack was stuck on the Sept 2025 servicing level); Docker engine and `pnpm db:up` (postgres + redis healthy) verified |
| 23 | API / security | BLOCKER: the OTP-logging dev mailer was chosen only by `RESEND_API_KEY` being unset (`apps/api/src/mailer.ts`); a production boot without the key would log every sign-in code as `[mail] to=… otp=…`. | Phase 1 review, 07 Sep 2026 | closed 07 Sep 2026: `createMailer` throws in production without RESEND_API_KEY and `loadConfig` requires the key in production (ADR-019); tests `[GAPS-23]` in mailer.test.ts and config.test.ts |
| 24 | API / security | Client IP for rate limits came from `cf-connecting-ip` / `x-forwarded-for` / `x-real-ip` without a trusted-proxy check, else `"unknown"` (`apps/api/src/security/context.ts`): spoofed headers bypassed the global, OTP and Better Auth limits; without a proxy all visitors shared one bucket. | Phase 1 review, 07 Sep 2026 | closed 07 Sep 2026: client IP = socket peer; forwarded headers honoured only for `TRUSTED_PROXY_IPS` peers; Better Auth reads the internal `x-hapiecoin-client-ip` header set after that check (ADR-019); tests `[GAPS-24]` in security.test.ts |
| 25 | API / config | `DATABASE_URL` and `REDIS_URL` stayed optional in production (`apps/api/src/config.ts`): an image started without them silently ran on in-memory PGlite and lost users/credentials on restart while `/healthz` reported 200. | Phase 1 review, 07 Sep 2026 | closed 07 Sep 2026: production requires DATABASE_URL (throws) and warns without REDIS_URL; test `[GAPS-25]` in config.test.ts |
| 26 | Web / auth | Referral input regex in `apps/web/src/lib/password.ts` (REF underscore plus 3+ characters) contradicted the server format `REF` + 7 base32 without underscore (`apps/api/src/auth.ts`, schema `ReferralCode`), so real codes were rejected and `referredBy` could never be set. | Phase 1 review, 07 Sep 2026 | closed 07 Sep 2026: web validates with the shared `ReferralCode` schema (`isReferralInput`), placeholder and message show the real format; tests in password.test.ts and AuthForms.test.tsx |
| 27 | API / auth | Google OAuth callback is `BETTER_AUTH_URL/callback/google` and `BETTER_AUTH_URL` defaulted to the API origin (`apps/api/src/auth.ts`, `env.example`), so the session cookie landed on the API origin and the web app (same-origin `/v1/me`, ADR-017/018) never saw it. | Phase 1 review, 07 Sep 2026 | closed 07 Sep 2026: `BETTER_AUTH_URL` defaults to `WEB_URL` so the OAuth callback rides the `/v1/*` rewrite and the cookie is first-party (ADR-019); e2e HC-PB-065 in auth.spec.ts drives the button through the mock provider; spec row HC-PB-065 |
| 28 | Gateway / feed | The 5-minute instrument refresh replaced the list but did not resubscribe new symbols for topics already held (`packages/venues/src/delta/market-data.ts`, `apps/gateway/src/feed/feed.ts`): strikes listed intraday showed up without quotes until every client left. | Phase 1 review, 07 Sep 2026 | closed 07 Sep 2026: `MarketFeed.rewatchHeldChains()` after every successful refresh; test `[GAPS-28]` in feed.test.ts |
| 29 | Spec / traceability | Chain tests were tagged `HC-CH-001` / `HC-CH-012`, IDs that did not exist in `spec/traceability.json`, so the Phase 1 gate could not close for the live chain (ADR-007); HC-SH-018/035/039 were implemented but untagged. | Phase 1 review, 07 Sep 2026 | closed 07 Sep 2026: spec rows HC-WS-107/108 (Phase 1 chain shell and rows) and HC-PB-065 (Google) added through `mockup-v2/features` + `spec/buildspec.js` (explicit `phase` override); chain tests retagged; HC-SH-018/035/039 tagged in the header and dialog tests |
| 30 | Minor batch | Reducer marks the chain stale on any `seq !== prev+1`, including `seq <= prev` (`apps/web/src/lib/gateway/reducer.ts`); `plan.ts` `desc(expiresAt)` sorts NULLs first; `apps/api/Dockerfile` does not copy `.npmrc` and ships PGlite in the production image; XAUT spot assumes a `XAUTUSD` perpetual the fixture does not contain (`feed.ts`); `/metrics` unauthenticated and no per-IP connection cap (`server.ts`); boot-time migrations race across replicas (`main.ts`); compose uses the floating `timescale/timescaledb:latest-pg17` tag; with `BETTER_AUTH_URL` = web origin, the API's own origin is no longer in the CORS allow-list, so Swagger "try it out" must be opened through the web proxy (`/v1/docs` on the web origin). | Phase 1 review, 07 Sep 2026 | closed 08 Sep 2026 (Phase 2 close-out) except the PGlite store size: reducer ignores replayed / out-of-order frames; `/v1/plan` orders active rows first and `expiresAt desc nulls last` (plan.test.ts); the API Dockerfile copies `.npmrc`; PGlite moved to devDependencies behind a dynamic import, so it is no longer a runtime dependency of the API and `require.resolve` fails inside the rebuilt image, but drizzle-orm still peer-links `@electric-sql+pglite@0.5.8` into the image's pnpm store (25 MB of a 403 MB image) because `pnpm deploy --prod` resolves peers from the full workspace install — still open as a size item, not a runtime one; XAUT spot verified live on 08 Sep (`/healthz` feed.spot.XAUT = 4425.33, the perpetual quote rides beside the 876 option instruments; only the test fixture lacks it); `/metrics` takes a bearer `METRICS_TOKEN` when set and each client address holds at most `MAX_CONNECTIONS_PER_IP` sockets (server.test.ts [GAPS-30]); Postgres migrations run under `pg_advisory_xact_lock` so replicas serialise; compose pins the database image by digest (timescaledb 2.29.2 / PG 17.11); Swagger "try it out" stays documented as web-origin `/v1/docs` |
| 31 | Gateway / venues | Open, High and Low are not in the gateway quote: the Delta option ticker adapter maps no candle fields (`packages/venues/src/delta/normalize.ts`, `schema-adapter.ts`), so the OHLC column group of HC-WS-021 is shown muted in Column Settings with "arrives with candle data (Phase 5)". Fix: carry `open`/`high`/`low` from the venue ticker through `Quote`, the gateway diff and the chain columns. | Phase 2 item 2, 07 Sep 2026 | open · partial: the 24 h spot high / low is served by `/v1/market/iv` (ADR-056); the gateway quote and the chain OHLC columns still lack candle fields |
| 32 | Web / chain | The option details dialog (HC-WS-026) has no 24 h mark / IV sparkline: the mock draws one from generated history and the app has no price history until the Phase 5 snapshotter. Fix: feed a per-instrument history endpoint from the snapshotter and draw the two lines on canvas. | Phase 2 item 3, 07 Sep 2026 | closed · ADR-056 (11 Sep 2026): `/v1/market/marks/{symbol}` + the 24 h mark / IV sparkline in the details dialog |
| 33 | Web / builder + pane minors | Parity details left out of Phase 2 item 4: payoff zoom is 1× / 2× / 4× steps instead of the mock's − 100 % + continuous zoom (HC-WS-036); the hover readout sits in the strip above the chart rather than a floating tooltip (HC-WS-045); the chain-picker expiry row scrolls natively without ‹ › buttons (HC-TR-028) and has no Live / Static toggle or column gear (HC-TR-029, 030); the live price cell does not flash on ticks (HC-TR-012); the spot-zone readout "Spot now ✓ profit zone" (HC-WS-046) is not shown. | Phase 2 item 4, 08 Sep 2026 | partly closed 08 Sep 2026 (Phase 2 close-out): ‹ › strip arrows on overflow (HC-WS-008, HC-TR-028), price flash (HC-TR-012) and the spot-zone readout (HC-WS-046) shipped with unit tests; still open: continuous zoom (HC-WS-036), floating hover tooltip (HC-WS-045), picker Live / Static toggle and column gear (HC-TR-029, 030) |
| 34 | Web / pane | Margin is estimated as the worst defined loss at expiry (ADR-023); the mock's exchange margin, ROI on margin and risk-as-%-of-capital rows (HC-WS-050, 051, 052, 058) need the broker margin call that arrives with Phase 3 trading. | Phase 2 item 4, 08 Sep 2026 | partly closed · ADR-029 (08 Sep 2026): Delta has no pre-trade margin endpoint, so the live preview shows the exchange's margin in use and available balance and keeps the worst-loss estimate; ROI-on-margin / risk-as-%-of-capital rows still use the estimate |
| 35 | Web / drafts | Drafts live in the browser (ADR-023): "Refresh strategies" (HC-TR-043) and "Activate" (HC-TR-046) have no server to talk to until the Phase 3 strategy API; a draft saved on one device is not on another. | Phase 2 item 4, 08 Sep 2026 | open (Phase 3) |
| 36 | Web / build | `next dev` (Turbopack) cannot resolve the `./x.js`-style imports of `@hapiecoin/pricing` sources through the `development` export condition, the same defect as `@hapiecoin/schema`; `next.config.ts` now aliases both packages to their built `dist`, so `pnpm --filter @hapiecoin/pricing build` must run before `next dev` (the turbo `dev` task does not depend on `^build`). Fix: give the packages a `development` condition Turbopack can follow (extension-less imports or a bundled dev entry). | Phase 2 item 4, 08 Sep 2026 | open |
| 37 | Trading | Phase 3 item 1 ships paper trading only: "Live trade", "Continue · Live", "Go live" and "Trade All → Live" are present but disabled with a note, `/v1/strategies/:id/start` refuses `mode: "live"` with 409, and the API stores `orderId` / `orderBatchId` columns that nothing fills. Item 2 (Delta India testnet, safeguards list in ADR-024) lifts all of them together. | Phase 3 item 1, 08 Sep 2026 | closed · Phase 3 item 2 (08 Sep 2026): all four paths place venue orders through `live/preview` + `live/place`; `/start` keeps refusing `mode: "live"` by design (ADR-025) |
| 38 | Trading / P&L | Paper P&L history (`strategy_pnl`) has an upsert route and the chart reads it, but no client tick writes a daily point yet, so the sparkline and history chart show `[0, live total]` until a scheduled writer (or the gateway snapshotter) records one point per day. | Phase 3 item 1, 08 Sep 2026 | closed · ADR-029 (08 Sep 2026): browser writer (`usePnlWriter`) upserts today's point 20 s after mount and every 5 min when moved; a server-side writer stays on the Phase 5 snapshotter list |
| 39 | Web / pane | After "Paper trade" the Builder empties, so the analysis pane shows "No strategy yet" while the position lives on the Paper tab; the mock behaves the same, but a trader will expect the pane to follow the selected paper strategy (payoff, Greeks, ladder at its entry premiums). Fix: a pane source switch (Builder legs | selected strategy) fed from `usePaperBook`. | Phase 3 item 1 visual capture, 08 Sep 2026 | closed · ADR-026 (08 Sep 2026): pane follows the selected card, auto-follows after trading, ticked net positions on the Live tab |
| 40 | Trading / live | Item 2 leaves two things for a follow-up (the positions row shipped 08 Sep, ADR-026, HC-TR-144/145): fills of pending orders are reconciled only by the manual Sync button (no venue WebSocket), and the preview's margin figure is the worst defined loss rather than the exchange's margin. None blocks a trade: pending orders show as pending until Sync, and the preview always shows wallet available. | Phase 3 item 2, 08 Sep 2026 | closed · ADR-029 (08 Sep 2026): background reconciler polls pending orders every 15 s and the Live tab polls while a chip is pending; margin in use shown in the preview; venue WebSocket remains a later optimisation |
| 41 | API / web contract | `GET /v1/brokers` answered `{ items }` while the web parses the list with the shared `paginated(Broker)` envelope, which requires `nextCursor`; the test mock sent `nextCursor: null`, so every Playwright run passed and the real app showed "No exchanges configured" in API Settings and Exchanges (found by the user on 08 Sep during the testnet trial). Fixed the same day: the route now declares and returns the shared envelope and its test asserts `nextCursor`. Lesson: list routes must use the schema package's envelope on both sides; a contract test against the real API for each web fetcher is still missing. | Manual test, 08 Sep 2026 | fixed (route); contract tests open |
| 42 | API / secrets | With `CREDENTIALS_ENC_KEY` unset the API generated a random vault key per process, so every dev restart (tsx watch on each code edit) made saved exchange keys undecryptable; the live preview then reported an opaque "Could not read the exchange wallet" (found by the user on 08 Sep on the testnet trial). Fixed the same day: development derives a stable key from `BETTER_AUTH_SECRET` (production still requires the explicit key), an undecryptable credential answers 409 "reconnect it in Settings → API Settings", and the preview logs and shows the wallet-read reason. The API Settings dialog now shows the reconnect warning in place and the header wallet chip reads "reconnect key" and opens the dialog (08 Sep, fix/currency-toggle-wallet). | Manual test, 08 Sep 2026 | fixed |
| 43 | Web / cache | The header wallet chip (ADR-028) shares the `live/positions` query with the Live tab; after Go live the tab showed "No open positions" for up to 10 s because the query was still fresh from page load and no order mutation invalidated it (caught by e2e HC-TR-063 on 08 Sep). Fixed the same day: every live mutation invalidates `["live", "positions"]` and the net-positions panel refetches on mount. Lesson: a query shared by a header widget must be invalidated by every mutation that changes it. | e2e, 08 Sep 2026 | fixed |
| 44 | Web / persistence | Raising `DEFAULT_LOTS` to 100 (ADR-028) did not reach browsers that had already persisted `chainLots: 10`, so the user still saw 10 after PR #10. Fixed: the store persists `lotsDefault` and lifts saved lots to the current default once; later choices stick. Lesson: a changed default for a persisted field needs a migration marker. | User test, 08 Sep 2026 | fixed |
| 45 | Web / settings | The header currency toggle (ADR-028) sent a partial `{ currency }` to `PUT /v1/settings`, whose schema is the whole strict settings object, so the API answered 400 and the toggle swallowed it (user test, 08 Sep). Fixed: the toggle sends the current settings with the currency flipped and toasts on error. Lesson: partial updates need a PATCH route or the full object; mutations must surface errors. | User test, 08 Sep 2026 | fixed |
| 46 | Product / referrals | Commission rows freeze the referrer's percentage at record time (ADR-031), so raising a referrer's `commission_pct` in admin does not restate their pending rows; the admin sees the new percentage in the Commissions table beside the old amounts (08 Sep). Open: decide whether pending rows should follow a percentage change (restate) or stay frozen (audit-friendly, current). |
| 47 | Product / admin | Impersonation (HC-AD-102) is not built (ADR-032): the reference site's button only toasts, and a real one needs a session-swap design (who, why, for how long, banner in the app, audit rows, no access to the user's exchange keys) and a consent rule. Open: decide whether support needs it at all; until then support asks the user for a screenshot or a share link. |
| 48 | Product / banners | The Banner Master table lists every banner on one page and images sit in Postgres as bytea (ADR-033). Fine for tens of banners; at hundreds add paging and move images to an object store behind the same `/v1/banners/{id}/image` URL (08 Sep). |
| 49 | Billing / ops | Razorpay webhooks need `RAZORPAY_WEBHOOK_SECRET` and a public API URL registered in the Razorpay dashboard (ADR-034); until then a payment whose tab closed before the confirm call stays Pending and support settles it by hand. Refunds and proration are not built (08 Sep). |
| 50 | Product / emails | Campaign sends run one address at a time inside the request (ADR-035); fine for hundreds, but a 5,000-recipient send would tie the request up for minutes and could hit the proxy timeout. Add a queue (Redis job + progress in History) before opening campaigns to the whole user base (08 Sep). |
| 51 | Test / e2e | `analyse.spec.ts` "legs and stripes survive a reload" failed once in a full Playwright run (the leg's side attribute was not yet on the chain row after reload) and passed on the immediate re-run of the spec (09 Sep). Likely a race between the persisted-legs restore and the first chain render; add an explicit wait on the legs count before asserting, and watch for repeats. |
| 52 | Product / assistant | The HapieCoin Assistant answers from a keyword bank in the browser (ADR-036); questions outside it get the fallback and the support email. A hosted model (API route, rate limit, "platform help only" guard, no strategy data leaving the session without consent) is a later item; the panel and the explainer stay as they are. | open |
| 53 | Public site | /payoff-preview draws synthetic open-interest bars and a fixed spot (ADR-037) because the public gateway does not expose instrument lists or chain snapshots to visitors. Live OI on the preview needs a public, rate-limited chain snapshot endpoint. | open |
| 54 | Public site | Landing extras from the v2 brief are not built (item 5c audit, 09 Sep 2026): the count-up stats animation, feature-tab auto-rotate, the terminal illustration's live top bar / scenario matrix / portfolio bar, the liquidation stat cells and 40-interval sparkline (need the Phase 5 analytics feed), and the IV figure on the Live Markets tiles. The reference parity rows (HC-PB-001..022) are all built; these are HapieCoin-only polish items. | open |
| 55 | Analytics / data | ETF flows, exchange BTC reserves, token unlocks, the Puell multiple and the gold / DXY tiles need paid or terms-restricted sources (ADR-038, research §2C–D). They ship as "coming soon" panels; unblocking needs a provider decision and budget. | open |
| 56 | Analytics / data | Binance `/futures/data/*` statistics keep only 30 days and CoinGecko Demo allows 10k calls/month (ADR-038). The 90D/1Y timeframe chips need an ingest-side history table (Postgres) fed from the daily snapshots; until then charts show the provider window. | open |
| 57 | Analytics / data | On the user's network `fapi.binance.com` and `www.okx.com` reset every TLS handshake (09 Sep 2026; curl `schannel: failed to receive handshake`, Node `ECONNRESET before secure TLS connection`), so the long-short and taker-volume jobs fail, the liquidation stream never connects and open interest / funding are Bybit-only. Add Bybit fallbacks to the ingest: `/v5/market/account-ratio` for the global long/short ratio, the `allLiquidation.{symbol}` public stream for liquidations, and a tickers job (mark + index price) that also unlocks the basis chart (ADR-040). Until then those panels say which venue they are waiting for. 09 Sep 2026 (ADR-041): long/short fallback and the Bybit liquidation stream shipped; still open: taker volume (OKX `rubik/stat/taker-volume`, units unverified because the OKX docs were unreachable) and the basis chart (needs a mark/index tickers dataset). | open (taker, basis) |
| 58 | Analytics / data | Per-coin liquidation history and the liquidation heatmap are built from the newest 100 captured events (ADR-040), so on a busy day they cover minutes, not the 12–24 h the panels name. The ingest should keep per-symbol hourly buckets (and a 5-minute ring for the 1 h/4 h windows) in the `liquidations` snapshot; the pages already say "from the captured events" and show the buffer's reach. | open |
| 59 | Analytics / data | Sentiment and options leftovers after PR 5.4a (ADR-042): AHR999 needs its published 200-day cost basis and age-curve constants verified from a source; the Puell multiple needs daily miner revenue (on-chain, paid or terms-restricted); the options page lists only Deribit and Delta India until OKX / Bybit / Binance option tickers are verified; the rainbow chart is a regression over the ingest's 1000-day window rather than the genesis-anchored curve. The pages say "n/a" or "coming soon" in each case. | open |
| 60 | Analytics / data | Whales (ADR-043) depend on Hyperliquid's undocumented leaderboard for wallet discovery; if it changes shape the job falls back to `WHALE_WALLETS` and the page says "watched wallets". Alerts are poll diffs (one minute), not fills; large orders come from Bybit only (Binance blocked from the ingest); exchange reserves remain coming soon (#55). A future improvement is Hyperliquid's WebSocket `userEvents` per tracked wallet for fill-level alerts. | open |
| 61 | Trading / live | Resting limit entries (ADR-044 A3) have no cancel / replace from the app: a limit that rests stays a pending order until Delta fills or cancels it; Sync records a cancel and Retry re-sends the same limit at its stored price, but the trader cannot pull or reprice it from the Live tab, and the card chips do not yet show the resting price (`StrategyOrder.limitPrice` carries it). Needs a cancel route on the executor and a "resting @ price · cancel" chip. | open | 09 Sep 2026 |
| 62 | Analyse / data | IV rank (HC-WS-096) and realised vs implied (HC-WS-097) need a per-instrument IV history and a 365-day underlying price series; nothing stores either yet (see #32). The Vol tab shows honest placeholders. Needs a snapshotter (ATM IV per expiry per day, spot closes) in the ingest service and a read route. | closed · ADR-056 (11 Sep 2026): API snapshotter (`iv_snapshots`, `instrument_marks`), `/v1/market/iv`, Vol tab rank + realised vs implied, header IV rank | 09 Sep 2026 |
| 63 | Analytics / data | The terminal's exchange pages (HC-MT-106) and the coin page's per-venue table have no 24 h volume per venue: the ingest reads open interest and funding per venue but no tickers. A small `tickers` job (Binance `/fapi/v1/ticker/24hr`, Bybit `/v5/market/tickers`, OKX `/api/v5/market/tickers`) would fill the tile, the column and the deferred basis chart (GAPS #57). | open |
| 64 | Alerts / delivery | Alerts (ADR-052) are evaluated only while a HapieCoin tab is open and delivered by in-app push (toast + browser notification) and email. Missing: a server-side evaluator on the gateway feed so alerts fire with the app closed (needs a price / IV consumer on the API and per-user strategy P&L there), Telegram delivery (bot + chat linking; the channel is shown disabled "soon"), and IV *rank* alerts (blocked on the IV history, #62). | ADR-052 review | closed · ADR-057 (11 Sep 2026): server-side evaluation after every IV snapshot (price, ATM IV, strategy P&L from marks) and Telegram delivery via a long-polled bot + /start linking; still open elsewhere: web push for a closed app, IV-rank alerts |
| 65 | Adjustment workbench | The Proposed list showed only legs picked from the chain; a trim, close or add on a held leg (lots after) appeared only as the pill on its row and in the Review count, although the approved H2 mockup says "the same order appears on the proposed list, where you can edit it too". Present since the A2 build; no test asserted the Proposed list for lots-after edits. | user test, 11 Sep | closed 11 Sep 2026 (fix/workbench-proposed): every order the change sends is a `wb-order` row under Proposed with side, mark, editable lots and remove; unit workbench.test.tsx checklist (trim → edit → close → add → remove), e2e analyse.spec.ts HC-TR-148 block |
| 66 | Trading / lifecycle | Nothing settles a paper or live leg past its expiry: it stays `open` with no mark, P&L freezes, no "expired" state, no expiry on the card or in the filters. | user review, 11 Sep | closed 11 Sep 2026 (roadmap A1 PR #47 + A3 `feat/trade-lifecycle-3`, ADR-059 §2.4): `apps/api/src/settlement.ts` books legs past the settlement instant at intrinsic value from the IV-snapshot spot nearest the instant (paper directly, live once the exchange shows the contract gone), archives with `closeReason: "expired"`; every close path records a reason (expired · squared off · closed outside the app; `stopped` arrives with A4); Closed chip cards and the Journal show it. Remaining: the settlement value is HapieCoin's snapshot spot, not Delta's official settlement price (the public API does not expose it); a leg with no snapshot within 30 min of the instant, or whose credential is gone, stays open, shows as out of sync again 15 min after the instant and waits for a manual Reconcile; any exchange-side close on an expiring contract (a stop, a liquidation, a manual close, even days earlier) that is not reconciled before the settler pass is booked as expired at intrinsic rather than at its real fill, and the reason then hides that a stop fired (the strategy stop of roadmap A4 knows its own fills; a venue order-history check is the general fix) |
| 67 | Live trading / safety | The app never compares the exchange's net position per contract with the strategies' open legs: a stop, a manual close or a liquidation on the exchange leaves the strategies out of date and nobody is told. Two strategies holding the same contract net into one position at the exchange, so an exchange-level stop closes both (the user lost a 800 + 200 lot position this way). No overlap warning before an order. | user report, 11 Sep | partly closed 11 Sep 2026 (roadmap A2): the overlap line in every preview and the workbench Review (HC-TR-159), the drift badge and banner on the Live tab (HC-TR-160), Reconcile that books lots closed outside the app with no orders (HC-TR-161; when two strategies share the contract the exchange cannot say whose lots went, so each card offers its own lots and the dialog says so; the trader books only what that strategy lost); still open: a server-side drift pass, Re-enter, a resting reduce-only exit that fills is closed by sync at the order level but its leg stays open until reconciled (its fill price should be the default there), and the strategy-level stop loss (§2.3, roadmap A4) |
| 68 | Web / mobile | The workspace is desktop-first: narrow handling exists in Workspace, ChainTable and AdjustWorkbench, but there is no `viewport` export, no `public/` directory (no manifest, icons or service worker), no touch handlers, and Playwright runs Desktop Chrome at 1440×900 only with no phone-width project. Chain rows rely on hover and the B / S keys, which do not exist on touch. Untested by real traders on phones. | platform audit, 10 Sep 2026 | open · roadmap E4 (viewport, manifest, service worker, Pixel 7 + iPhone 14 projects, tap targets), then a Capacitor shell (ADR-060) |
| 69 | Repo / CI | There is no `.github/` directory: no CI workflow runs `pnpm ci`, the bundle budget or e2e on a push or PR; no dependabot / renovate, no secret scanning, no LICENSE, no SBOM. The strict coverage gates only run when someone types `pnpm ci` locally (docs/deploy.md lists the workflow as a to-do). | platform audit, 10 Sep 2026 | closed 11 Sep 2026 (ADR-061): `.github/workflows/ci.yml` runs `pnpm ci`, the bundle budget and the Playwright e2e on every PR and push to main, and builds the api and gateway images on main; `smoke.yml` on manual dispatch; `dependabot.yml` weekly for the actions; the npm ecosystem is switched off because Dependabot's updater cannot fetch the pnpm 12 binary (first main run 11 Sep 2026 failed on `pnpm update --lockfile-only`), so npm bumps stay manual (`pnpm outdated`) until Dependabot supports pnpm 12; `LICENSE` (proprietary) + `license: UNLICENSED`. Still out: `prettier --check` (480 files unformatted), SBOM / secret scanning |
| 70 | API / security | No global request body limit (only the 5 MB banner upload in routes/banners.ts is capped) and no per-route rate limit on order placement: `/v1/strategies/{id}/live/place`, `/live/batch` and `/live/positions/exit` share the 300 req/min global bucket in security/rate-limit.ts. | platform audit, 10 Sep 2026 | closed 11 Sep 2026 (ADR-061): `security/body-limit.ts` caps every body at `API_BODY_LIMIT_BYTES` (1 MB) before guards and validators, 413 `PAYLOAD_TOO_LARGE`, banner upload exempt (keeps 5 MB); `orderRateLimit` 20/min per user (`ORDER_RATE_MAX_PER_MIN`) on live place / retry / batch / positions exit, 429 `ORDER_RATE_LIMITED`; tests in security.test.ts and config.test.ts. Follow-up: the same budget on `/v1/strategies/{id}/adjust`, `/legs/{legId}/close` and `/close` (all in routes/strategies.ts, which the A2 branch edits; the two close routes send reduce-only exits) once that branch lands; the budget counts requests, and one batch request may carry up to 20 strategies |
| 71 | API / infra | Every background job runs inside the API process (main.ts:76-97): only the IV snapshotter has an off-switch (`IV_SNAPSHOT_MS=0`) for extra replicas; the reconciler and the Telegram long-poller would double-run on a second API replica. Together with #16 (per-process gateway fan-out) the stack is single-replica today. | platform audit, 10 Sep 2026 | closed 11 Sep 2026 (ADR-062): the reconciler, IV snapshotter (+ alerts) and Telegram linker start through `startJobs` under the Redis lease `hapiecoin:leader:api-jobs` (`API_JOBS_ROLE` leader / always / off, `LEADER_TTL_MS`); a standby replica takes over within the lease; `/healthz` reports `jobs`; the memory lock keeps a one-copy deployment unchanged. Tests: jobs.test.ts, leader.test.ts |
| 72 | Ops / observability | No error tracker, APM or API metrics: the API logs JSON through pino and exposes `/healthz`; only the gateway serves Prometheus metrics. docs/deploy.md §5 leaves the tracker as an open decision. | platform audit, 10 Sep 2026 | open · roadmap F27 |
| 73 | API / auth | No TOTP second factor: better-auth is configured with email OTP, passkeys and optional Google only. The live-order confirmation (`TradeModeDialog.tsx`, `BatchLiveDialog.tsx`) is a plain button, not a typed confirmation, for an account that places real orders. | platform audit, 10 Sep 2026 | open · roadmap F28 (better-auth TOTP plugin; typed LIVE confirmation) |
| 74 | Builder / templates | A template cannot hold a perpetual future leg: `TemplateLeg.k` is strike-row based, so futures-only and futures + options structures (covered call, protective put, collar, covered put) are missing from the catalogue although futures are first-class in the schema, the pricing engine and the Builder's Add Futures dialog. Tests hard-code the count of 28 in three places. | platform audit, 10 Sep 2026 | closed 10 Sep 2026 (PR #50, roadmap D17) |
| 75 | Builder / templates | No test asserts each template's textbook shape: max profit and max loss defined or unbounded, breakeven count and the outlook it expresses. The pricing tests prove the maths on hand-built legs; the template tests prove placement on the ladder; nothing joins them per template. | platform audit, 10 Sep 2026 | closed 10 Sep 2026 (PR #52, roadmap D19) |
| 76 | Builder / templates | Template cards and the loader only hold the chosen expiry's chain: a leg placed on a later expiry (`expiryOffset`, the two calendars since ADR-023 and the diagonal / double diagonal / calendar strangle since ADR-060) is materialised from the base rows, so its mark and IV are the near expiry's until the Builder re-prices it, and the card's POP / R:R would be computed on a payoff the engine values leg by leg at its own expiry (a flat debit line). Since ADR-060 the stats hook skips calendar-family templates and the card says "Figures need the later expiry loaded"; the loader still falls back. Needs a second chain subscription for `expiries[i + 1]` feeding `rowsByExpiry`, and a target-date (near expiry) valuation for calendar figures (ADR-044 `valuationMs`). | code review, 10 Sep 2026 | closed 10 Sep 2026: PR #48 (ec15a61) subscribes the template loader to the next listed expiry's chain (`rowsByExpiry`, `farSeq`) and values multi-expiry positions at the nearest expiry (`nearestExpiryValuationMs`, ADR-059 addendum); the template stack (test/templates-invariants) makes `materialiseTemplate` refuse a far leg without its chain, passes `rowsByExpiry` and `valuationMs` into the stats hook, and asserts every calendar template's hump in templates.invariants.test.ts |
| 77 | Trading / margin | The margin estimate (ADR-023: worst defined loss at expiry) understates a calendar or diagonal: the exchange margins each expiry on its own, so the short near leg is margined as if naked (CoinGreeks showed ₹10,091 for a calendar whose worst loss is ₹615). | user review, 11 Sep | open · the preview labels the estimate as understated across expiries and the live preview shows the exchange's own figure at placement; a per-expiry margin model needs Delta's formula |
| 78 | Tests / flakiness | `apps/web/src/components/analytics/analytics-pages2.test.tsx` line 120 asserts the premium chart's empty note synchronously right after the page state settles; under the full parallel run (88 workers) it failed once on 11 Sep 2026 and passed on every rerun, alone and in the full suite. Fix: wrap that assertion in `waitFor` like the line above it. Second symptom (11 Sep 2026, twice in the stop hook's `pnpm test` while the dev stack was running): a vitest fork running `src/components/header/AppHeader.test.tsx` died with Windows exit code 3221226505 (STATUS_STACK_BUFFER_OVERRUN fast-fail) after every test in it passed; the file passes alone every time and the full suite passed 579/579 in the same hour. Environmental (88 forked jsdom workers plus the dev stack, 16 cores / 16 GB); `apps/web/vitest.config.ts` and `packages/ui/vitest.config.ts` now cap `maxWorkers` at 50 % (the ui Preview file died with "Zone Allocation failed" at one fork per core), after which both suites passed twice in a row with the dev stack running (11 Sep); the root `pnpm test` script now runs one package at a time (`--concurrency=1`): all twelve at once hit "JavaScript heap out of memory" with about 4 GB free, and at three at a time the api's PGlite forks still died beside the web forks; every package passes alone (CI runs `pnpm run ci`, unaffected). The line-120 `waitFor` fix is still to do. | Low | Open |
| 79 | Web / venues | The web client carries its own copy of the Delta specifics the venue port now owns (ADR-063): the symbol formatter `apps/web/src/lib/strategy/legs.ts` `deltaSymbol` and parser `positions.ts` `parseDeltaSymbol`, the lot-size literal in `lib/adjust/useAdjustWorkbench.ts`, the settlement rule in `lib/chain/expiries.ts` and seven `"delta_india"` literals in five files. `port.test.ts` pins the formatter to the same vectors, so the copies cannot drift silently, but they move to the port only when the strategy carries a `venue` (E20 step 2). | venue port build, 11 Sep 2026 | closed 11 Sep 2026 (ADR-064: `@hapiecoin/venues/core`, `venueSymbol`, `parseVenueSymbol`, `defaultLotSizes`, the venue calendar, `DEFAULT_VENUE`) |
| 80 | Tests / flakiness | PR #65's first CI run failed with every web test green: sonner's `deleteToast` schedules a 200 ms `removeToast` timer that is never cleared, so a toast closing late in a file's last test (admin users, 4.1 s on the runner) set state after jsdom was torn down and vitest reported an unhandled `window is not defined`. Fix: `apps/web/test/setup.ts` tracks real timers created during a test and clears the pending ones after each test. | CI run, 11 Sep 2026 | closed 11 Sep 2026 (PR #65) |
